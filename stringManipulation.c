#include<stdio.h>
#include<string.h>

int main(){
	char name[] = "Mia";
	printf("The length of thr string is: %d",strlen(name));
char dad[] = "Anthony";
printf("\nMy two names are: ",name.strcat(dad));
printf("%s to uppercase is : %s",name,strupr(name));
}

